<script src="{{ asset('js/nav.js') }}"></script>

{% include "_nav.html.twig" %}

<header id="header">
    <div id="nav-opener-container">
        <a id="nav-opener" href="javascript:void(0)" onclick="openNav()"><i class="bi bi-list"></i> Menu</a>
    </div>
    <div id="logo-container">
        <a href="{{ path('home') }}"><img id="logo" src="{{ asset('images/big_gra.png') }}" alt="Logo"></a>
        <a href="{{ path('home') }}" id="app-title">Ptero Dactyl<span class="blink">_</span></a>
    </div>

    <div id="theme-switcher-container">
        <button id="theme-switcher" title="Changer de thème">
            <i class="bi"></i>
        </button>
    </div>
</header>

<style>
.blink {
    font-family: inherit;
    font-size: inherit;
    animation-name: anim-blink;
    animation-duration: 1s;
    animation-direction: alternate;
    animation-iteration-count: infinite;
    animation-timing-function: linear;
}
@keyframes anim-blink {
    from { opacity: 100%; }
    to { opacity: 0%; }
}

header {
    height: 80px;
    background-color: var(--dark-bg-primary);
    color: var(--dark-fg);
    display: flex;
    align-items: stretch;
    justify-content: space-between;
    box-shadow: 0 0 10px var(--dark-bg-primary);
    transition: margin-left 0.5s;
}

nav {
    width: 0;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 1;
    background-color: var(--dark-bg-primary);
    overflow-x: hidden;
    margin: 90px 10px 10px 10px;
    transition: 0.2s;
    box-shadow: 0px 0 10px var(--dark-bg-primary);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: stretch;
    border-radius: 5px;
}

#nav-opener-container {
    flex: 1;
    display: flex;
    justify-content: flex-start;
    align-items: center;
    min-width: 100px;
    max-width: 15%;
}

nav > #links {
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: stretch;
}

nav > #links > a, nav > #links > a:visited {
    text-decoration: none;
    color: var(--dark-fg);
    padding: 10px;
    width: 250px;
}

nav > #links > a:hover {
    background-color: var(--dark-bg-secondary);
}

nav > #version {
    font-size: small;
    margin: 10px;
    transform: translateY(-10px);
    text-align: right;
}

#nav-opener, #nav-opener:hover, #nav-opener:visited {
    color: var(--dark-fg);
    text-decoration: none;
    padding: 10px;
    margin: 10px;
    border-radius: 5px;
}

#logo-container {
    flex: 2;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
}

#logo {
    height: 80px;
}

@media all and (max-width: 600px) {
    #app-title, #app-title:active, #app-title:hover, #app-title:visited {
        display: none;
    }
}

#app-title, #app-title:active, #app-title:hover, #app-title:visited {
    color: #282828;
    font-family: "Iosevka Etoile Extrabold", serif;
    font-size: 3em;
    -webkit-text-stroke-width: 2px;
    -webkit-text-stroke-color: var(--dark-fg);
    text-decoration: none;
}

#theme-switcher-container {
    flex: 1;
    display: flex;
    justify-content: flex-end;
    max-width: 15%;
}

#theme-switcher {
    border:none;
    background: none;
    font-size: 1em;
    margin: 20px;
    transition-duration: 0.1s;
}

#theme-switcher:hover {
    cursor: pointer;
    transform: scale(1.5) rotate(-33deg);
}

.red-switcher {
    color: var(--orange);
}

.red-switcher:hover {
    color: var(--blue);
}

.blue-switcher {
    color: var(--blue);
}

.blue-switcher:hover {
    color: var(--orange);
}

</style>
